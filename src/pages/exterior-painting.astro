---
import BaseLayout from '../layouts/BaseLayout.astro';
import ServiceSchema from '../components/ServiceSchema.astro';

const title = "Exterior Painting Services | Coastal Weather-Resistant Painting Alabama";
const description = "Expert exterior painting services for South Alabama homes. Weather-resistant, salt air protection. Serving Gulf Shores, Orange Beach, Fairhope, Mobile.";
const keywords = "exterior painting Alabama, coastal painting, weather resistant paint, beach house painting";
---

<BaseLayout
  title={title}
  description={description}
  keywords={keywords}
>
  <main>
    <!-- Service Schema -->
    <ServiceSchema
      serviceName="Exterior Painting Services"
      serviceDescription="Expert exterior painting services for South Alabama homes. Weather-resistant, salt air protection. Serving Gulf Shores, Orange Beach, Fairhope, Mobile."
      serviceUrl="https://callbrownspainting.com/exterior-painting"
      serviceType="Exterior Painting"
      priceRange="$$"
      serviceImage="https://callbrownspainting.com/assets/exterior-painting-service.jpg"
    />

    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-background">
        <div class="hero-slide active" style="background-image: url('/images/hero/hero-1-optimized.jpg')"></div>
        <div class="hero-slide" style="background-image: url('/images/projects/projects-optimized/interior-painting-2-optimized.jpg')"></div>
        <div class="hero-slide" style="background-image: url('/images/hero/hero-3-optimized.jpg'); background-position: center top;"></div>
      </div>
      
      <div class="hero-overlay"></div>
      
      <div class="hero-trust-badges">
        <div class="trust-badge">
          <span class="trust-stars">★★★★★</span>
          <span class="trust-text">5-Star Reviews</span>
        </div>
        <div class="trust-badge">
          <span class="trust-icon">🏆</span>
          <span class="trust-text">25+ Years</span>
        </div>
        <div class="trust-badge">
          <span class="trust-icon">📋</span>
          <span class="trust-text">Licensed & Insured</span>
        </div>
        <div class="trust-badge">
          <span class="trust-icon">✅</span>
          <span class="trust-text">100% Guaranteed</span>
        </div>
      </div>

      <div class="container">
        <div class="hero-content">
          <div class="hero-text">
            <img src="/images/logo/browns-painting-logo-hat.png" alt="Browns Painting" class="hero-logo-image" />
            <h1>Professional Exterior Painting Services</h1>
            <p class="hero-subtitle">Protect and beautify your property with expert exterior painting throughout South Alabama</p>
            <div class="phone-number">
              <span class="phone-label">Call Us Today at</span>
              <a href="tel:+12512374545" class="phone-link">(251) 237-4545</a>
            </div>
            <div class="hero-cta">
              <a href="/contact" class="btn btn-primary">
                <span class="btn-icon">📋</span>
                Get A Free Quote
              </a>
            </div>
          </div>
        </div>
      </div>
      
      <div class="hero-dots">
        <span class="dot active" data-slide="0"></span>
        <span class="dot" data-slide="1"></span>
        <span class="dot" data-slide="2"></span>
      </div>
    </section>

    <!-- Services Section -->
    <section class="services-section">
      <div class="container">
        <h2>Our Exterior Painting Services</h2>
        <div class="services-grid">
          <div class="service-card">
            <h3>🏠 House Painting</h3>
            <p>Complete exterior house painting with weather-resistant coatings for Alabama's climate.</p>
            <ul>
              <li>Siding painting</li>
              <li>Trim & fascia</li>
              <li>Shutters & doors</li>
              <li>Garage doors</li>
            </ul>
          </div>
          
          <div class="service-card">
            <h3>🌊 Coastal Protection</h3>
            <p>Specialized paint systems designed to withstand salt air and coastal conditions.</p>
            <ul>
              <li>Salt air resistant coatings</li>
              <li>UV protection</li>
              <li>Moisture barriers</li>
              <li>Mildew resistance</li>
            </ul>
          </div>
          
          <div class="service-card">
            <h3>🪵 Deck & Fence</h3>
            <p>Professional deck staining and fence painting with long-lasting protection.</p>
            <ul>
              <li>Deck staining</li>
              <li>Fence painting</li>
              <li>Porch painting</li>
              <li>Pressure washing included</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq-section">
      <div class="container">
        <h2>Frequently Asked Questions - Exterior Painting</h2>
        <div class="faq-container">
          <div class="faq-item">
            <h3>How long does exterior painting last in Alabama?</h3>
            <p>With proper preparation and premium paint, exterior painting typically lasts 7-10 years in Alabama. Coastal properties may need repainting every 5-7 years due to salt air exposure. We use specialized coatings for coastal areas to maximize longevity.</p>
          </div>
          <div class="faq-item">
            <h3>What's the best time of year for exterior painting?</h3>
            <p>Spring and fall are ideal for exterior painting in Alabama, with mild temperatures and lower humidity. However, we can paint year-round by monitoring weather conditions and choosing appropriate days for optimal paint curing.</p>
          </div>
          <div class="faq-item">
            <h3>Do you include pressure washing?</h3>
            <p>Yes! Pressure washing is included with all exterior painting projects. Proper surface cleaning is essential for paint adhesion and longevity. We thoroughly clean all surfaces before any painting begins.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
      <div class="container">
        <h2>Ready for Professional Exterior Painting?</h2>
        <p>Contact Barry "Boomer" Brown today for a free estimate on your exterior painting project.</p>
        <a href="/contact" class="btn btn-primary">
          <span class="btn-icon">📞</span>
          Get Free Estimate
        </a>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* Hero Section */
  .hero {
    position: relative;
    height: 100vh;
    min-height: 600px;
    overflow: hidden;
    display: flex;
    align-items: center;
    color: white;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .hero-slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    opacity: 0;
    transition: opacity 1s ease-in-out;
  }

  .hero-slide.active {
    opacity: 1;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.6) 0%, rgba(0, 0, 0, 0.4) 100%);
    z-index: 2;
  }

  .hero-content {
    position: relative;
    z-index: 3;
    width: 100%;
    text-align: center;
  }

  .hero-text {
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem;
  }

  .hero-logo-image {
    height: 300px;
    width: auto;
    max-width: 100%;
    object-fit: contain;
    display: block;
    margin: 0 auto;
    margin-bottom: 0;
  }

  .hero h1 {
    font-size: clamp(3rem, 5vw, 4rem);
    font-weight: 800;
    margin: 0;
    margin-top: -4rem;
    margin-bottom: 1rem;
    line-height: 1.1;
    color: white;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.95);
    margin-bottom: 1.5rem;
    line-height: 1.6;
  }

  .phone-number {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255, 255, 255, 0.9);
    font-size: 1rem;
    justify-content: center;
    margin: 0;
  }

  .phone-label {
    font-weight: 500;
  }

  .phone-link {
    color: #fbbf24;
    text-decoration: none;
    font-weight: 700;
    font-size: 1.125rem;
  }

  .phone-link:hover {
    color: #f59e0b;
    text-decoration: underline;
  }

  .hero-cta {
    margin-top: 2rem;
  }

  .hero-trust-badges {
    position: absolute;
    top: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 1.5rem;
    z-index: 4;
    flex-wrap: wrap;
    justify-content: center;
  }

  .trust-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.94rem 1.56rem;
    background: transparent;
    color: white;
    font-size: 1.125rem;
    font-weight: 600;
    transition: all 0.3s ease;
    white-space: nowrap;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .trust-badge:hover {
    transform: translateY(-2px);
    text-shadow: 2px 4px 8px rgba(0, 0, 0, 0.9);
  }

  .trust-badge .trust-icon {
    font-size: 1.56rem;
    color: #fbbf24;
  }

  .trust-badge .trust-stars {
    font-size: 1.56rem;
    color: #fbbf24;
    font-weight: 700;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .trust-badge .trust-text {
    font-size: 1.06rem;
    font-weight: 600;
  }

  .hero-dots {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 0.5rem;
    z-index: 3;
  }

  .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.5);
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .dot.active {
    background: #fbbf24;
    transform: scale(1.2);
  }

  .dot:hover {
    background: rgba(255, 255, 255, 0.8);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1.25rem 2.5rem;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 700;
    transition: all 0.3s ease;
    font-size: 1.125rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .btn-primary {
    background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
    color: #1f2937;
    box-shadow: 
      0 8px 25px rgba(251, 191, 36, 0.4),
      inset 0 1px 0 rgba(255, 255, 255, 0.3);
    position: relative;
    overflow: hidden;
  }

  .btn-primary::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    transition: left 0.5s ease;
  }

  .btn-primary:hover::before {
    left: 100%;
  }

  .btn-primary:hover {
    background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    transform: translateY(-3px);
    box-shadow: 
      0 12px 35px rgba(251, 191, 36, 0.6),
      inset 0 1px 0 rgba(255, 255, 255, 0.4);
  }

  .btn-icon {
    font-size: 1.25rem;
  }

  /* Content Sections */
  .services-section,
  .faq-section {
    padding: 5rem 0;
  }

  .services-section {
    background: #f8fafc;
  }

  .faq-section {
    background: white;
  }

  .services-section h2,
  .faq-section h2 {
    text-align: center;
    font-size: 2.5rem;
    font-weight: 800;
    color: #1f2937;
    margin-bottom: 3rem;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-top: 3rem;
  }

  .service-card {
    background: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    border-left: 4px solid #3b82f6;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
  }

  .service-card h3 {
    color: #1e40af;
    margin-bottom: 1rem;
    font-size: 1.5rem;
    font-weight: 700;
  }

  .service-card p {
    margin-bottom: 1rem;
    line-height: 1.7;
    color: #4b5563;
  }

  .service-card ul {
    margin: 1rem 0;
    padding-left: 1.5rem;
  }

  .service-card li {
    margin-bottom: 0.5rem;
    color: #6b7280;
    line-height: 1.6;
  }

  .faq-container {
    display: grid;
    gap: 1.5rem;
    margin-top: 3rem;
  }

  .faq-item {
    background: #f8fafc;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
    border-left: 4px solid #3b82f6;
    transition: transform 0.3s ease;
  }

  .faq-item:hover {
    transform: translateX(4px);
  }

  .faq-item h3 {
    color: #1e40af;
    margin-bottom: 1rem;
    font-size: 1.25rem;
    font-weight: 700;
  }

  .faq-item p {
    line-height: 1.7;
    color: #4b5563;
    margin: 0;
  }

  .cta-section {
    text-align: center;
    padding: 5rem 2rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
  }

  .cta-section h2 {
    margin-bottom: 1rem;
    font-size: 2.5rem;
    font-weight: 800;
  }

  .cta-section p {
    margin-bottom: 2rem;
    font-size: 1.125rem;
    opacity: 0.95;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

    /* Mobile Responsive */
  @media (max-width: 768px) {
    .container {
      padding: 0 1rem !important;
    }

    .hero {
      height: 80vh !important;
      min-height: 500px !important;
      padding: 8rem 0 4rem !important;
    }

    .hero-logo-image {
      height: 180px !important;
      margin-bottom: 1rem !important;
    }

    .hero-subtitle {
      font-size: 1rem !important;
      margin-bottom: 1rem !important;
      padding: 0 0.5rem !important;
    }

    .hero-text {
      padding: 1rem !important;
    }

    .hero h1 {
      font-size: 2rem !important;
      margin-top: -2rem !important;
      margin-bottom: 1rem !important;
    }

    .phone-number {
      font-size: 0.9rem !important;
    }

    .phone-link {
      font-size: 1rem !important;
    }

    .hero-trust-badges {
      top: 0.75rem !important;
      gap: 0.5rem !important;
      padding: 0 0.5rem !important;
      max-width: 100% !important;
      display: grid !important;
      grid-template-columns: 1fr 1fr !important;
      justify-items: center !important;
    }

    .trust-badge {
      padding: 0.5rem 0.75rem !important;
      font-size: 0.75rem !important;
      white-space: nowrap !important;
      width: 100% !important;
      max-width: 160px !important;
      justify-content: center !important;
    }

    .trust-badge .trust-icon {
      font-size: 1rem !important;
    }

    .trust-badge .trust-stars {
      font-size: 1rem !important;
    }

    .trust-badge .trust-text {
      font-size: 0.75rem !important;
    }

    .intro-section h2,
    .services-section h2,
    .service-areas-section h2,
    .faq-section h2 {
      font-size: 1.75rem !important;
    }

    .intro-section p,
    .service-areas-section > p {
      font-size: 1rem !important;
    }

    .services-grid {
      grid-template-columns: 1fr !important;
      gap: 1.5rem !important;
    }

    .service-card {
      padding: 1.5rem !important;
    }

    .service-card h3 {
      font-size: 1.25rem !important;
    }

    .areas-list {
      grid-template-columns: 1fr !important;
      gap: 1.5rem !important;
    }

    .area-group {
      padding: 1.5rem !important;
    }

    .area-group h3 {
      font-size: 1.25rem !important;
    }

    .faq-grid {
      gap: 1rem !important;
    }

    .faq-item {
      padding: 1.5rem !important;
    }

    .faq-item h3 {
      font-size: 1.125rem !important;
    }

    .btn {
      padding: 1rem 2rem !important;
      font-size: 1rem !important;
      width: 100% !important;
      max-width: 280px !important;
    }

    .services-section,
    .service-areas-section,
    .faq-section {
      padding: 2.5rem 0 !important;
    }

    .intro-section {
      padding: 2.5rem 0 !important;
    }

    .cta-section {
      padding: 2.5rem 1rem !important;
    }

    .cta-section h2 {
      font-size: 1.75rem !important;
    }

    .cta-section p {
      font-size: 1rem !important;
    }
  }

  @media (max-width: 480px) {
    .hero {
      padding: 7rem 0 3rem !important;
      min-height: 75vh !important;
    }

    .hero-logo-image {
      height: 150px !important;
    }

    .hero h1 {
      font-size: 1.75rem !important;
      margin-top: -1.5rem !important;
    }

    .hero-trust-badges {
      top: 0.5rem !important;
      gap: 0.4rem !important;
    }

    .trust-badge {
      padding: 0.4rem 0.6rem !important;
      font-size: 0.7rem !important;
      max-width: 140px !important;
    }

    .trust-badge .trust-icon,
    .trust-badge .trust-stars {
      font-size: 0.9rem !important;
    }

    .trust-badge .trust-text {
      font-size: 0.7rem !important;
    }

    .service-card,
    .area-group,
    .faq-item {
      padding: 1.25rem !important;
    }

    .services-section,
    .service-areas-section,
    .faq-section,
    .intro-section {
      padding: 2rem 0 !important;
    }

    .cta-section {
      padding: 2rem 0.75rem !important;
    }
  }
</style>

<script>
  // Hero Slider Functionality
  document.addEventListener('DOMContentLoaded', function() {
    const slides = document.querySelectorAll('.hero-slide');
    const dots = document.querySelectorAll('.dot');
    let currentSlide = 0;
    let slideInterval;

    if (!slides.length || !dots.length) return;

    function showSlide(index) {
      // Remove active class from all slides and dots
      slides.forEach(slide => slide.classList.remove('active'));
      dots.forEach(dot => dot.classList.remove('active'));
      
      // Add active class to current slide and dot
      slides[index].classList.add('active');
      dots[index].classList.add('active');
      
      currentSlide = index;
    }

    function nextSlide() {
      const next = (currentSlide + 1) % slides.length;
      showSlide(next);
    }

    function startSlider() {
      if (slideInterval) clearInterval(slideInterval);
      slideInterval = setInterval(nextSlide, 5000); // Change slide every 5 seconds
    }

    function stopSlider() {
      if (slideInterval) clearInterval(slideInterval);
    }

    // Add click/touch event listeners to dots
    dots.forEach((dot, index) => {
      const handleDotClick = () => {
        showSlide(index);
        stopSlider();
        startSlider(); // Restart the auto-slider
      };
      
      dot.addEventListener('click', handleDotClick);
      dot.addEventListener('touchend', (e) => {
        e.preventDefault();
        handleDotClick();
      });
    });

    // Touch swipe support
    const hero = document.querySelector('.hero');
    if (hero) {
      let touchStartX = 0;
      let touchEndX = 0;

      hero.addEventListener('touchstart', (e) => {
        touchStartX = e.changedTouches[0].screenX;
      }, { passive: true });

      hero.addEventListener('touchend', (e) => {
        touchEndX = e.changedTouches[0].screenX;
        handleSwipe();
      }, { passive: true });

      function handleSwipe() {
        const swipeThreshold = 50;
        if (touchEndX < touchStartX - swipeThreshold) {
          // Swipe left - next slide
          nextSlide();
          stopSlider();
          startSlider();
        }
        if (touchEndX > touchStartX + swipeThreshold) {
          // Swipe right - previous slide
          const prev = (currentSlide - 1 + slides.length) % slides.length;
          showSlide(prev);
          stopSlider();
          startSlider();
        }
      }

      // Pause slider on hover (desktop only)
      hero.addEventListener('mouseenter', stopSlider);
      hero.addEventListener('mouseleave', startSlider);
    }

    // Start the slider
    startSlider();
  });
</script>
