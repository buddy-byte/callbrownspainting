---
import BaseLayout from '../layouts/BaseLayout.astro';
import ServiceSchema from '../components/ServiceSchema.astro';

const title = "Professional House Painter | Residential Painting Services Alabama";
const description = "Expert house painters serving South Alabama. Complete residential painting services. Licensed, insured, locally owned. Serving Mobile, Baldwin, Escambia counties.";
const keywords = "house painter Alabama, residential painting, home painters, local painters";
---

<BaseLayout
  title={title}
  description={description}
  keywords={keywords}
>
  <main>
    <!-- Service Schema -->
    <ServiceSchema
      serviceName="House Painter Services"
      serviceDescription="Expert house painters serving South Alabama. Complete residential painting services. Licensed, insured, locally owned. Serving Mobile, Baldwin, Escambia counties."
      serviceUrl="https://callbrownspainting.com/house-painter"
      serviceType="House Painter"
      priceRange="$$"
      serviceImage="https://callbrownspainting.com/assets/house-painter-service.jpg"
    />

    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-background">
        <div class="hero-slide active" style="background-image: url('/images/hero/hero-1-optimized.jpg')"></div>
        <div class="hero-slide" style="background-image: url('/images/projects/projects-optimized/interior-painting-2-optimized.jpg')"></div>
        <div class="hero-slide" style="background-image: url('/images/hero/hero-3-optimized.jpg'); background-position: center top;"></div>
      </div>
      
      <div class="hero-overlay"></div>
      
      <div class="hero-trust-badges">
        <div class="trust-badge">
          <span class="trust-stars">★★★★★</span>
          <span class="trust-text">5-Star Reviews</span>
        </div>
        <div class="trust-badge">
          <span class="trust-icon">🏆</span>
          <span class="trust-text">25+ Years</span>
        </div>
        <div class="trust-badge">
          <span class="trust-icon">📋</span>
          <span class="trust-text">Licensed & Insured</span>
        </div>
        <div class="trust-badge">
          <span class="trust-icon">✅</span>
          <span class="trust-text">100% Guaranteed</span>
        </div>
      </div>

      <div class="container">
        <div class="hero-content">
          <div class="hero-text">
            <img src="/images/logo/browns-painting-logo-hat.png" alt="Browns Painting" class="hero-logo-image" />
            <h1>Professional House Painting Services</h1>
            <p class="hero-subtitle">Transform your home with expert house painting services throughout South Alabama</p>
            <div class="phone-number">
              <span class="phone-label">Call Us Today at</span>
              <a href="tel:+12512374545" class="phone-link">(251) 237-4545</a>
            </div>
            <div class="hero-cta">
              <a href="/contact" class="btn btn-primary">
                <span class="btn-icon">📋</span>
                Get A Free Quote
              </a>
            </div>
          </div>
        </div>
      </div>
      
      <div class="hero-dots">
        <span class="dot active" data-slide="0"></span>
        <span class="dot" data-slide="1"></span>
        <span class="dot" data-slide="2"></span>
      </div>
    </section>

    <!-- Services Section -->
    <section class="services-section">
      <div class="container">
        <h2>Our House Painter Services</h2>
        <div class="services-grid">
          <div class="service-card">
            <h3>🏠 Complete Home Painting</h3>
            <p>Full-service residential painting for every area of your home.</p>
            <ul>
              <li>Interior painting</li>
              <li>Exterior painting</li>
              <li>Trim & molding</li>
              <li>Ceiling painting</li>
            </ul>
          </div>
          
          <div class="service-card">
            <h3>🎨 Custom Finishes</h3>
            <p>Specialized painting techniques and custom finishes for unique spaces.</p>
            <ul>
              <li>Accent walls</li>
              <li>Texture finishes</li>
              <li>Faux painting</li>
              <li>Decorative techniques</li>
            </ul>
          </div>
          
          <div class="service-card">
            <h3>🔧 Repairs & Prep</h3>
            <p>Complete preparation and repair services for perfect results.</p>
            <ul>
              <li>Drywall repair</li>
              <li>Wood rot repair</li>
              <li>Caulking & sealing</li>
              <li>Surface preparation</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="faq-section">
      <div class="container">
        <h2>Frequently Asked Questions - House Painter</h2>
        <div class="faq-container">
          <div class="faq-item">
            <h3>What makes a good house painter?</h3>
            <p>A professional house painter should have proper licensing and insurance, use quality materials, provide detailed estimates, communicate clearly, and have strong local references. Browns Painting has 25+ years of experience and hundreds of satisfied Alabama homeowners.</p>
          </div>
          <div class="faq-item">
            <h3>How much does it cost to paint a house?</h3>
            <p>House painting costs vary based on size, condition, and scope. Interior painting typically ranges from $2.50-$4.50 per square foot, while exterior painting runs $1.80-$3.50 per square foot. We provide free, detailed estimates with no hidden costs.</p>
          </div>
          <div class="faq-item">
            <h3>Are you licensed and insured?</h3>
            <p>Yes, Browns Painting is fully licensed and insured to work throughout South Alabama. We carry comprehensive liability insurance and workers' compensation coverage for your protection and peace of mind.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="cta-section">
      <div class="container">
        <h2>Ready for Professional House Painter?</h2>
        <p>Contact Barry "Boomer" Brown today for a free estimate on your house painter project.</p>
        <a href="/contact" class="btn btn-primary">
          <span class="btn-icon">📞</span>
          Get Free Estimate
        </a>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  /* Hero Section */
  .hero {
    position: relative;
    height: 100vh;
    min-height: 600px;
    overflow: hidden;
    display: flex;
    align-items: center;
    color: white;
  }

  .hero-background {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .hero-slide {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    opacity: 0;
    transition: opacity 1s ease-in-out;
  }

  .hero-slide.active {
    opacity: 1;
  }

  .hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, rgba(0, 0, 0, 0.6) 0%, rgba(0, 0, 0, 0.4) 100%);
    z-index: 2;
  }

  .hero-content {
    position: relative;
    z-index: 3;
    width: 100%;
    text-align: center;
  }

  .hero-text {
    max-width: 800px;
    margin: 0 auto;
    padding: 2rem;
  }

  .hero-logo-image {
    height: 300px;
    width: auto;
    max-width: 100%;
    object-fit: contain;
    display: block;
    margin: 0 auto;
    margin-bottom: 0;
  }

  .hero h1 {
    font-size: clamp(3rem, 5vw, 4rem);
    font-weight: 800;
    margin: 0;
    margin-top: -4rem;
    margin-bottom: 1rem;
    line-height: 1.1;
    color: white;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.95);
    margin-bottom: 1.5rem;
    line-height: 1.6;
  }

  .phone-number {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255, 255, 255, 0.9);
    font-size: 1rem;
    justify-content: center;
    margin: 0;
  }

  .phone-label {
    font-weight: 500;
  }

  .phone-link {
    color: #fbbf24;
    text-decoration: none;
    font-weight: 700;
    font-size: 1.125rem;
  }

  .phone-link:hover {
    color: #f59e0b;
    text-decoration: underline;
  }

  .hero-cta {
    margin-top: 2rem;
  }

  .hero-trust-badges {
    position: absolute;
    top: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 1.5rem;
    z-index: 4;
    flex-wrap: wrap;
    justify-content: center;
  }

  .trust-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.94rem 1.56rem;
    background: transparent;
    color: white;
    font-size: 1.125rem;
    font-weight: 600;
    transition: all 0.3s ease;
    white-space: nowrap;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .trust-badge:hover {
    transform: translateY(-2px);
    text-shadow: 2px 4px 8px rgba(0, 0, 0, 0.9);
  }

  .trust-badge .trust-icon {
    font-size: 1.56rem;
    color: #fbbf24;
  }

  .trust-badge .trust-stars {
    font-size: 1.56rem;
    color: #fbbf24;
    font-weight: 700;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.8);
  }

  .trust-badge .trust-text {
    font-size: 1.06rem;
    font-weight: 600;
  }

  .hero-dots {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    gap: 0.5rem;
    z-index: 3;
  }

  .dot {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.5);
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .dot.active {
    background: #fbbf24;
    transform: scale(1.2);
  }

  .dot:hover {
    background: rgba(255, 255, 255, 0.8);
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1.25rem 2.5rem;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 700;
    transition: all 0.3s ease;
    font-size: 1.125rem;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .btn-primary {
    background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
    color: #1f2937;
    box-shadow: 
      0 8px 25px rgba(251, 191, 36, 0.4),
      inset 0 1px 0 rgba(255, 255, 255, 0.3);
    position: relative;
    overflow: hidden;
  }

  .btn-primary::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    transition: left 0.5s ease;
  }

  .btn-primary:hover::before {
    left: 100%;
  }

  .btn-primary:hover {
    background: linear-gradient(135deg, #f59e0b 0%, #d97706 100%);
    transform: translateY(-3px);
    box-shadow: 
      0 12px 35px rgba(251, 191, 36, 0.6),
      inset 0 1px 0 rgba(255, 255, 255, 0.4);
  }

  .btn-icon {
    font-size: 1.25rem;
  }

  /* Content Sections */
  .services-section,
  .faq-section {
    padding: 5rem 0;
  }

  .services-section {
    background: #f8fafc;
  }

  .faq-section {
    background: white;
  }

  .services-section h2,
  .faq-section h2 {
    text-align: center;
    font-size: 2.5rem;
    font-weight: 800;
    color: #1f2937;
    margin-bottom: 3rem;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin-top: 3rem;
  }

  .service-card {
    background: white;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    border-left: 4px solid #3b82f6;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
  }

  .service-card h3 {
    color: #1e40af;
    margin-bottom: 1rem;
    font-size: 1.5rem;
    font-weight: 700;
  }

  .service-card p {
    margin-bottom: 1rem;
    line-height: 1.7;
    color: #4b5563;
  }

  .service-card ul {
    margin: 1rem 0;
    padding-left: 1.5rem;
  }

  .service-card li {
    margin-bottom: 0.5rem;
    color: #6b7280;
    line-height: 1.6;
  }

  .faq-container {
    display: grid;
    gap: 1.5rem;
    margin-top: 3rem;
  }

  .faq-item {
    background: #f8fafc;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
    border-left: 4px solid #3b82f6;
    transition: transform 0.3s ease;
  }

  .faq-item:hover {
    transform: translateX(4px);
  }

  .faq-item h3 {
    color: #1e40af;
    margin-bottom: 1rem;
    font-size: 1.25rem;
    font-weight: 700;
  }

  .faq-item p {
    line-height: 1.7;
    color: #4b5563;
    margin: 0;
  }

  .cta-section {
    text-align: center;
    padding: 5rem 2rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
  }

  .cta-section h2 {
    margin-bottom: 1rem;
    font-size: 2.5rem;
    font-weight: 800;
  }

  .cta-section p {
    margin-bottom: 2rem;
    font-size: 1.125rem;
    opacity: 0.95;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    .hero {
      height: 70vh;
      min-height: 500px;
    }

    .hero-logo-image {
      height: 200px;
    }

    .hero h1 {
      font-size: 2rem;
      margin-top: -2rem;
    }

    .hero-subtitle {
      font-size: 1.1rem;
    }

    .hero-trust-badges {
      top: 1rem;
      gap: 0.75rem;
      padding: 0 1rem;
    }

    .trust-badge {
      padding: 0.63rem 1.25rem;
      font-size: 1rem;
    }

    .trust-badge .trust-icon,
    .trust-badge .trust-stars {
      font-size: 1.25rem;
    }

    .trust-badge .trust-text {
      font-size: 0.94rem;
    }

    .services-grid {
      grid-template-columns: 1fr;
    }

    .btn {
      padding: 1rem 1.5rem;
      font-size: 1rem;
    }

    .services-section,
    .faq-section {
      padding: 3rem 0;
    }

    .cta-section {
      padding: 3rem 1.5rem;
    }
  }
</style>

<script>
  // Hero Slider Functionality
  document.addEventListener('DOMContentLoaded', function() {
    const slides = document.querySelectorAll('.hero-slide');
    const dots = document.querySelectorAll('.dot');
    let currentSlide = 0;
    let slideInterval;

    function showSlide(index) {
      slides.forEach(slide => slide.classList.remove('active'));
      dots.forEach(dot => dot.classList.remove('active'));
      slides[index].classList.add('active');
      dots[index].classList.add('active');
      currentSlide = index;
    }

    function nextSlide() {
      const next = (currentSlide + 1) % slides.length;
      showSlide(next);
    }

    function startSlider() {
      slideInterval = setInterval(nextSlide, 5000);
    }

    function stopSlider() {
      clearInterval(slideInterval);
    }

    dots.forEach((dot, index) => {
      dot.addEventListener('click', () => {
        showSlide(index);
        stopSlider();
        startSlider();
      });
    });

    startSlider();

    const hero = document.querySelector('.hero');
    hero.addEventListener('mouseenter', stopSlider);
    hero.addEventListener('mouseleave', startSlider);
  });
</script>
